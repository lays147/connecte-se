{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *.ts) /home/lays/.local/bin/mise exec -- npm run test:e2e ;; esac; }",
            "timeout": 120,
            "statusMessage": "Running e2e tests..."
          }
        ]
      }
    ]
  }
}
